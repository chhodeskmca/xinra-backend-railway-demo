{
  "name": "xinra-backend",
  "version": "1.0.0",
  "description": "Xinra backend API for authentication and role-based user management.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "postinstall": "prisma generate && prisma migrate deploy && node prisma/seed.js",
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "check": "find src prisma -name '*.js' -print0 | xargs -0 -n1 node --check",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "seed": "node prisma/seed.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1033.0",
    "@aws-sdk/s3-request-presigner": "^3.1033.0",
    "@prisma/client": "^6.0.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1",
    "prisma": "^6.0.0",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
